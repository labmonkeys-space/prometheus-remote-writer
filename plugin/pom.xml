<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.opennms.plugins</groupId>
        <artifactId>prometheus-remote-writer-parent</artifactId>
        <version>0.7.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>prometheus-remote-writer</artifactId>
    <packaging>bundle</packaging>
    <name>OpenNMS Prometheus Remote Writer :: Plugin</name>

    <dependencies>
        <!-- OpenNMS Integration API v2.0 (Apache 2.0) -->
        <dependency>
            <groupId>org.opennms.integration.api</groupId>
            <artifactId>api</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.opennms.integration.api</groupId>
            <artifactId>common</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Embedded runtime dependencies -->
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>okhttp-jvm</artifactId>
        </dependency>
        <dependency>
            <groupId>org.xerial.snappy</groupId>
            <artifactId>snappy-java</artifactId>
        </dependency>
        <dependency>
            <groupId>org.json</groupId>
            <artifactId>json</artifactId>
        </dependency>
        <dependency>
            <groupId>com.google.protobuf</groupId>
            <artifactId>protobuf-java</artifactId>
        </dependency>
        <dependency>
            <groupId>io.dropwizard.metrics</groupId>
            <artifactId>metrics-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.dropwizard.metrics</groupId>
            <artifactId>metrics-jmx</artifactId>
        </dependency>

        <!-- Logging: provided by Karaf at runtime -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- OSGi: provided by the framework at runtime -->
        <dependency>
            <groupId>org.osgi</groupId>
            <artifactId>osgi.core</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.osgi</groupId>
            <artifactId>osgi.cmpn</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.karaf.shell</groupId>
            <artifactId>org.apache.karaf.shell.core</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>mockwebserver</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Integration-test support for the TSS compliance suite (JUnit 4 based).
             Version managed in the parent pom. -->
        <dependency>
            <groupId>org.opennms.integration.api</groupId>
            <artifactId>tss-tests</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.vintage</groupId>
            <artifactId>junit-vintage-engine</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <extensions>
            <extension>
                <groupId>kr.motd.maven</groupId>
                <artifactId>os-maven-plugin</artifactId>
                <version>${os-maven-plugin.version}</version>
            </extension>
        </extensions>
        <plugins>
            <!--
                Attach the default properties file as a classified artifact so
                the Karaf feature can pull it via
                mvn:org.opennms.plugins/prometheus-remote-writer/<ver>/cfg/config
                and drop it into $KARAF_ETC on install.
            -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.6.1</version>
                <executions>
                    <execution>
                        <id>attach-default-cfg</id>
                        <phase>package</phase>
                        <goals><goal>attach-artifact</goal></goals>
                        <configuration>
                            <artifacts>
                                <artifact>
                                    <file>${project.basedir}/src/main/resources/org.opennms.plugins.tss.prometheusremotewriter.cfg</file>
                                    <type>cfg</type>
                                    <classifier>config</classifier>
                                </artifact>
                            </artifacts>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.xolstice.maven.plugins</groupId>
                <artifactId>protobuf-maven-plugin</artifactId>
                <configuration>
                    <protocArtifact>
                        com.google.protobuf:protoc:${protobuf.version}:exe:${os.detected.classifier}
                    </protocArtifact>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <configuration>
                    <instructions>
                        <Bundle-SymbolicName>org.opennms.plugins.prometheus-remote-writer</Bundle-SymbolicName>
                        <Bundle-Name>${project.name}</Bundle-Name>
                        <Bundle-Version>${project.version}</Bundle-Version>
                        <Bundle-License>https://www.apache.org/licenses/LICENSE-2.0</Bundle-License>
                        <Bundle-Blueprint>OSGI-INF/blueprint/*.xml</Bundle-Blueprint>
                        <!--
                            Karaf's CommandExtender inspects ONLY bundles that
                            carry this header; without it the @Command/@Service
                            annotations are never scanned and the shell command
                            does not exist (#113). Narrow package form per
                            OpenNMS core/health/shell precedent.
                        -->
                        <Karaf-Commands>org.opennms.plugins.prometheus.remotewriter.shell*</Karaf-Commands>
                        <Export-Package />
                        <Import-Package>
                            <!--
                                A hand-written range here is kept ONLY while the
                                runtime fact that requires it still holds. An
                                inert override with an expired reason is worse
                                than none: the next reader reasons from it.

                                org.osgi.framework and org.osgi.service.cm were
                                pinned for Horizon 35 (Felix 6.0.5 = OSGi R6 =
                                framework 1.9) while the build compiled against
                                osgi.core 8.0.0. Horizon 36.0.4 ships Felix
                                7.0.5 exporting framework 1.10 and configadmin
                                1.9.26 exporting cm 1.6.0, so bnd's computed
                                ranges resolve unaided and both overrides were
                                removed.
                            -->
                            <!--
                                Pin the Integration API floor explicitly.
                                bnd computes [2.0,3) from the compiled-against
                                version, which is WIDER than what we support:
                                it would let this bundle wire against the
                                2.0.0 shipped by Horizon 35.x-36.0.3 and fail
                                later at runtime. Declaring the real floor
                                turns that into an install-time resolution
                                failure instead. Keep in sync with the
                                feature range in karaf-features and with
                                opennms-integration-api.version in the parent
                                pom — `make verify-compat` enforces it.
                            -->
                            org.opennms.integration.api.v1.timeseries.*;version="[2.0.1,3)",
                            org.apache.karaf.shell.api.action;version="[4.3,5)",
                            org.apache.karaf.shell.api.action.lifecycle;version="[4.3,5)",
                            <!--
                                slf4j: STILL LOAD-BEARING, unlike the OSGi
                                ranges above. Horizon 36.0.4 ships
                                lib/slf4j-api-1.7.36.jar while we compile
                                against 2.x, so without this bnd writes a
                                2.x floor the container cannot satisfy. The
                                1.7→2.0 boundary is API-compatible for our
                                usage (Logger / LoggerFactory / MDC), so
                                accept either.
                            -->
                            org.slf4j;version="[1.7,3)",
                            org.slf4j.event;version="[1.7,3)";resolution:=optional,
                            org.slf4j.helpers;version="[1.7,3)";resolution:=optional,
                            org.slf4j.spi;version="[1.7,3)";resolution:=optional,
                            javax.net.ssl.*,
                            javax.xml.*,
                            *;resolution:=optional
                        </Import-Package>
                        <Embed-Dependency>
                            okhttp-jvm;scope=compile,
                            okio-jvm;scope=compile,
                            kotlin-stdlib;scope=compile,
                            snappy-java;scope=compile,
                            protobuf-java;scope=compile,
                            metrics-core;scope=compile,
                            metrics-jmx;scope=compile,
                            json;scope=compile
                        </Embed-Dependency>
                        <Embed-Transitive>true</Embed-Transitive>
                        <Embed-Directory>lib</Embed-Directory>
                        <Include-Resource>
                            {maven-resources},
                            {maven-dependencies}
                        </Include-Resource>
                    </instructions>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
