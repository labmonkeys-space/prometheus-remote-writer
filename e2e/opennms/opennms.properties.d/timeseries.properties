# The gateway no longer runs in-process: OpenNMS keeps its own time series
# store (default rrd) and the Kafka Producer forwards a copy of collected
# metrics to Kafka, which the standalone gateway consumes and fans out to the
# Prometheus-compatible backend.
org.opennms.timeseries.strategy=rrd
