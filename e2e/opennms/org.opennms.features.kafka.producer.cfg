# OpenNMS Kafka Producer — forward collected metrics as CollectionSetProtos.
# `forward.metrics` enables metric forwarding (off by default); `metricTopic`
# names the topic the gateway consumes (default is also `metrics`).
forward.metrics=true
metricTopic=metrics
