# Read-only v2c agent for the e2e sandbox. OpenNMS's default SNMP config
# (community "public", v2c) discovers and collects it without configuration.
rocommunity public
sysName snmpd-1
sysLocation e2e-sandbox
sysContact ops@example.invalid
agentaddress udp:161
